generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["metrics", "multiSchema", "relationJoins", "strictUndefinedChecks"]
}

datasource db {
    provider     = "sqlite"
    url          = env("DATABASE_URL")
    relationMode = "prisma"
}
